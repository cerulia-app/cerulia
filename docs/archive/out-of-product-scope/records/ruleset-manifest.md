# ruleset-manifest

この record は Cerulia の active design では廃止した。ruleset の選択は `rulesetNsid` と `character-sheet-schema` の明示選択で表現する。historical context が必要な場合だけこの文書を参照し、active な rules provenance は `character-sheet-schema` と `rule-profile` を正本とする。